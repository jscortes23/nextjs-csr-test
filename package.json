{
  "name": "nextjs-csr",
  "private": true,
  "description": "Next.js 14 Example",
  "version": "0.0.0",
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "start": "pnpm -r start",
    "e2e:ci": "pnpm run build"
  },
  "dependencies": {
    "concurrently": "8.2.2"
  }
}
